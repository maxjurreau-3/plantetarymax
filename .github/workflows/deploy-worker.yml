name: Deploy Worker
on:
  push:
    branches:
      - align/master-spec-2
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install Wrangler
        run: npm install -g wrangler
      - name: Set Worker secret EVENTS_PUBLISH_TOKEN
        env:
          EVENTS_PUBLISH_TOKEN: ${{ secrets.EVENTS_PUBLISH_TOKEN }}
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
        run: |
          # Pipe the secret into wrangler so it can be stored non-interactively
          printf '%s' "$EVENTS_PUBLISH_TOKEN" | wrangler secret put EVENTS_PUBLISH_TOKEN --env production
      - name: Publish Worker
        env:
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
        run: |
          echo "$CF_API_TOKEN" > /tmp/cf_token && chmod 600 /tmp/cf_token || true
          wrangler publish --env production
